{
  "name": "cursor",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "start:win": "set NODE_ENV=production && node server.js",
    "dev": "node server.js",
    "build": "vite build",
    "postinstall": "npm run build || echo 'Build failed, continuing anyway'",
    "preview": "vite preview"
  },
  "dependencies": {
    "express": "^4.18.2",
    "three": "^0.161.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3",
    "vite": "^5.1.4"
  }
} 